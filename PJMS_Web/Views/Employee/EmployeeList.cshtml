
@{
    ViewBag.Title = "EmployeeList";
    Layout = "~/Views/Shared/_PJMSLayout.cshtml";
}
<div class="panel panel-inverse">
    <div class="panel-heading">
        <h4 class="panel-title">スタッフ一覧</h4>
        <div class="panel-heading-btn">
            <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default" data-click="panel-expand"><i class="fa fa-expand"></i></a>
        </div>
    </div>
    <div class="panel-body">
        <table id="tblEmployee" class="table table-striped table-bordered table-td-valign-middle" style="width:100%">
            <thead>
                <tr>
                    <th>画像</th>
                    <th>スタッフCD</th>
                    <th>スタッフ名</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
        <!-- Creates the bootstrap modal where the image will appear -->
        <div class="modal fade" id="imagemodal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content" style="width: 535px;">
                    <div class="modal-header">
                        <h4 class="modal-title" id="myModalLabel">Image preview</h4>
                        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                    </div>
                    <div class="modal-body">
                        <img src="" id="imagepreview" style="width: 500px; height: 500px;" >
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
    {
    <script type="text/javascript">
        $(document).ready(function () {
            var obj = {
                EmployeeCD: ''
            };
            var response = CalltoApiController('@Url.Action("GetEmployee", "api/EmployeeListApi")',obj);
            BindTable(response);
            //$("img").on("click", function (data) {
            //    //var imgSrc = "http://163.43.113.92/HR_Management/Photo/" + data + ".jpg";
            //    var imgSrc = $('#pop img').attr('src');
            //    $('#imagepreview').attr('src',this);
            //    $('#imagemodal').modal('show');
            //});

            $('#pop img').on("click", function () {
                $('#imagepreview').prop('src', this.src);
                $('#imagemodal').modal('show');
                //var imgSrc = $(this).attr('src');
                //var imgSrc = $(this).closest('img').attr('src')
                //$('#imagepreview').attr('src', imgSrc);
                
            });

        });
        function BindTable(response) {
            table = $('#tblEmployee').DataTable({
                responsive: true,
                data: JSON.parse(response),
                datasrc:"",
                destroy: true,
                "paging":true,
                "ordering": true,
                "columns": [
                    { "data": "EmployeePhoto", "className": "align-center", "width": "13%" },
                    { "data": "EmployeeCD", "className": "align-center", "width": "20%" },
                    { "data": "EmployeeName", "className": "align-center", "width": "67%" },
                ],
                "columnDefs": [{
                    "targets": 0,
                    "data": "EmployeePhoto",
                    "render": function (data) {
                        return '<a href="#" id="pop"><img class="imageresource" src="http://163.43.113.92/HR_Management/Photo/' + data + '" style="max-width:50px; max-height:70px" /></a >'; 
                    },
                }]
            });
        }
        
        @*function EmployeeEdit(data, mode) {
            window.location.href = '@Url.Action("EmployeeEntry", "Employee")?EmployeeCD=' + data + '&Mode=' + mode;
        }*@
    </script>
} 